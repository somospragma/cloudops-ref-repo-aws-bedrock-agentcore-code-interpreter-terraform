# Data sources are prohibited in Reference Modules (PC-IAC-011).
# All external data (VPC IDs, Subnet IDs, Security Group IDs, IAM Role ARNs)
# must be resolved in the Root module and injected via variables.
